# Core scientific computing packages
numpy==2.2.6
pandas==2.2.3
matplotlib==3.10.0
scipy==1.15.3
scikit-learn==1.7.1

# Data processing and serialization
pyarrow==19.0.0
# fastparquet>=2024.2.0  # Not currently installed
openpyxl==3.1.5

# Serial communication (MCU controller)
pyserial==3.5

# Audio processing
sounddevice==0.5.2
soundfile==0.13.1

# Object storage (S3-compatible)
minio==7.2.16

# Configuration management
python-dotenv==1.1.0


# Build tools and packaging
setuptools==72.1.0

# Binary data parsing - Kaitai Struct (using local version in src/kaitaistruct)
-e src/kaitaistruct
# git+https://github.com/kaitai-io/kaitai_struct_python_runtime

# Note: The following are Python standard library modules and don't need to be listed:
# tkinter, threading, datetime, os, io, subprocess, socket, asyncio, re, enum,
# itertools, sys, struct, warnings

# EVAL-CN0582-USBZ DAQ (HelpFunctions/cn0582.py)
libusb1>=3.1          # async bulk streaming, required for gapless capture
pyusb>=1.2            # control-only fallback
